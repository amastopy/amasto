# Copilot Instructions — amasto

## Notes

- Use the `uv` command for dependency management; do not directly edit the `dependencies` section in `pyproject.toml`.
- Keep `README.md` always up to date.
